export interface Player {
    /**
     * The player's current x position (in tiles), bottom left = 0, 0.
     */
    x: number;
    /**
     * The player's current y position (in tiles), bottom left = 0, 0.
     */
    y: number;
}
